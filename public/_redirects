# Handle Next.js routes
/_next/* /_next/:splat 200

# Handle API routes
/api/* /api/:splat 200

# Handle specific routes for orders and settings
/orders /orders 200!
/settings /settings 200!

# SPA fallback - this should be the last rule
/* /index.html 200
